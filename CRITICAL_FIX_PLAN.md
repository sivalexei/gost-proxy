# Критическая проблема: Деобфускация обнуляет первые 8 байт (counter + length)

## Симптом

**Клиент** отправляет DATA-пакет с:
- counter = 2 → первые 4 байта: `00 00 00 02`
- total_length (tl) = 85...93 → байты 4-7

**Сервер** получает RECV_PAYLOAD:
```
00 00 00 00 00 00 00 00 41 fb c9 28 d4 03 b6 e0 ...
```
Первые 8 байт = ВСЕГДА нули. Деобфускация не восстанавливает их.

**Результат**: `protocol_unpack_data` читает tl=0 → отклоняет пакет.

## Данные расследования

| Поле | Клиент (PACK_DATA) | Сервер (recv) |
|------|---------------------|---------------|
| obf_key | `6052ee67 4a4a0cef 321f1f59 ba3507bb` | `6052ee67 4a4a0cef 321f1f59 ba3507bb` ✅ |
| hdr | `47 4f 53 54 02 00 00 00 01 ...` | `47 4f 53 54 02 00 00 00 01 ...` ✅ |
| payload[0..7] | `00 00 00 02 00 00 00 55` | `00 00 00 00 00 00 00 00` ❌ |
| payload[8..] | зашифровано/обфусцировано | `41 fb c9 28 d4 03 b6 e0 ...` |

Ключи и заголовки совпадают → проблема не в `obf_key_derive` или `hdr`.

## Корневая причина

**КРИТИЧЕСКОЕ НАБЛЮДЕНИЕ**: Клиент отправляет `len=1433` в QUIC, но `sizeof(gost_packet_t)`:
- header = 16 байт (magic 4 + type 1 + padding 3 + conn_id 4 + session_id 8 + ... нет, packet_t это структура)

Нужно проверить размеры. `gost_packet_t` вероятно содержит:
- magic(4) + type(1) + padding(3) + conn_id(4) + session_id(8) + auth_tag(16) + payload[MAX_PAYLOAD-4]
- Это ~1424 байта

Клиент отправляет 1433 → 9 байт смещение!

## План исправления

### Этап 1: Hex-dump сырых данных

1. В `session.c` после `obfuscate_payload` → hex-dump `pkt->payload[0..15]`
2. В `server.c` в `handle_packet` ДО `pkt = (gost_packet_t*)data` → hex-dump `data[0..15]`
3. Сравнить — если разные, проблема в QUIC layer или сетевой передаче

### Этап 2: Исправить размер пакета

- Проверить `sizeof(gost_packet_t)` в `gost_common.h`
- Убедиться что клиент отправляет ровно `sizeof(gost_packet_t)` байт
- Если отправляется `1433` вместо `1424` → исправить размер отправки

### Этап 3: Проверить обфускацию на отдельном тесте

- Написать standalone C-файл который:
  - Создаёт payload с `counter=2, tl=93`
  - Считает `hdr` и `obf_key` из session_id
  - Вызывает `obfuscate_payload` → hex-dump результата
  - Вызывает `deobfuscate_payload` → hex-dump результата
  - Сравнивает с оригиналом
- Это подтвердит симметричность обфускации

### Этап 4: Исправление ✅ **ВЫПОЛНЕНО**

**Корневая причина**: клиент обфусцировал только `8+tl` байт (`obfuscate_payload(pkt->payload, 8+tl, ...)`),
а сервер деобфусцировал `8+MAX_PAYLOAD-4` байт (`deobfuscate_payload(deobf, 8+MAX_PAYLOAD-4, ...)`).

Поток обфускации (`idx`) останавливался после `8+tl` байт на клиенте, но сервер продолжал читать
c теми же индексами stream — рассинхрон. При малых `tl` деобфускация затрагивала мусорные байты хвоста
и искажала counter/length.

**Решение**: клиент теперь обфусцирует весь диапазон `8+MAX_PAYLOAD-4`, как и сервер деобфусцирует.
Файл: `src/core/session.c` → `protocol_pack_data()` строка 112.

**Результат**: standalone-тест `test_obf_simple.c` подтвердил:
| Сценарий | Ошибки |
|---|---|
| Старый баг (8+108 vs 8+1396) | 730 |
| Фикс (8+1396 vs 8+1396) | 0 |
